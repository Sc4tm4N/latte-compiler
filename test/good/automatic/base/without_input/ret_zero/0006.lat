// print int variables according to int min (compared to C)
int main() {
  int INT_MIN = -2147483648;
  printInt(INT_MIN + INT_MIN);
  printInt(INT_MIN - INT_MIN);
  printInt(INT_MIN + 2 * INT_MIN);
  printInt(INT_MIN - 2 * INT_MIN);
  printInt(7 * INT_MIN);
  printInt(INT_MIN * 7);
  printInt(INT_MIN / 7);
  printInt(7 / INT_MIN);
  printInt(7 % INT_MIN);
  printInt(INT_MIN % 7);
  printInt(INT_MIN * INT_MIN);
  printInt(INT_MIN * INT_MIN * (INT_MIN * INT_MIN) / INT_MIN - INT_MIN * INT_MIN);
  printInt(INT_MIN * INT_MIN * (INT_MIN * INT_MIN) / INT_MIN + INT_MIN * INT_MIN + 5);
  return 0;
}